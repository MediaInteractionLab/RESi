import React from 'react';
import MatrixVisualization from './MatrixVisualization';

const types = [
    { name: 'matrix', component: <MatrixVisualization width={500} height={500} /> },
];

export default types;
